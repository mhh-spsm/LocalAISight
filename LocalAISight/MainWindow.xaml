<Window x:Class="LocalAISight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LocalAISight"
        mc:Ignorable="d"
        Title="LocalAISight" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Knappraden -->
                <RowDefinition Height="Auto"/>
                <!-- Rubrik + modellval -->
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center" Margin="10">
                <Button x:Name="TargetButton" Content="Välj fönster" Click="TargetButton_Click" Margin="5" Padding="10"/>
                <Button x:Name="CaptureButton" Content="Beskriv!" Click="CaptureButton_Click" Margin="5" Padding="10"/>
                <Button x:Name="SettingsButton" Content="Inställningar" Click="SettingsButton_Click" Margin="5" Padding="10"/>
                <ComboBox x:Name="ProfilesCombo" Width="200" Margin="5" SelectionChanged="ProfilesCombo_SelectionChanged" AutomationProperties.Name="Välj profil:">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"
                       AutomationProperties.Name="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Button x:Name="ManageProfilesButton" Content="Profiler" Click="ManageProfilesButton_Click" Margin="5" Padding="10"/>
            </StackPanel>
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="DescriptionLbl" Text="Beskrivning"
                           FontSize="16"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           AutomationProperties.HeadingLevel="Level2"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Label Content="AI-modell:" 
                           Target="{Binding ElementName=ModelCombo}" 
                           VerticalAlignment="Center" 
                           Margin="0,0,6,0"
                           AutomationProperties.Name="Välj AI-modell"/>
                    <ComboBox x:Name="ModelCombo"
                              Width="220"
                              ItemsSource="{Binding AvailableModels}"
                        SelectedItem="{Binding SelectedModel, Mode=TwoWay}"
                        Margin="0"
                              AutomationProperties.Name="AI-modell"/>
                </StackPanel>
            </Grid>
        </Grid>
        <ScrollViewer Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="DescriptionBox" 
                 TextWrapping="Wrap" 
                 Focusable="True"
                 IsReadOnlyCaretVisible="True" 
                 AutomationProperties.LiveSetting="Polite"
                 IsReadOnly="True"
                 AutomationProperties.Name="Beskrivningar"
                 Text="Väntar på första beskrivningen..."
                 Background="#F0F0F0"
                 Padding="10"/>
        </ScrollViewer>

        <DockPanel Grid.Row="2" Margin="10">
            <TextBox x:Name="UserQuestionBox" AutomationProperties.Name="Skriv din fråga här" VerticalContentAlignment="Center" Height="35" GotFocus="UserQuestionBox_GotFocus" />
            <Button x:Name="AskButton" Content="Skicka Fråga" Click="AskButton_Click" DockPanel.Dock="Right" Padding="15,0" Margin="10,0,0,0" IsDefault="True"/>
        </DockPanel>
    </Grid>
</Window>
